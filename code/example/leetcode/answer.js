const { B, C } = require(__toolname);
const { DATA_TYPE_ENUM } = __config;

const config = {
  type: DATA_TYPE_ENUM.DEFAULT,
  fnName: 'Leetcode',
  params: ['a', 'b', 'c']
}

const answer = (...param) => {
  B();
  C;
}

module.exports = {
  config,
  answer
}